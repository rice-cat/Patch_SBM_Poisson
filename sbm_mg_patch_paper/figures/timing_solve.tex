\begin{tikzpicture}
    \begin{scope}
        \begin{axis}[
                width=0.45\columnwidth,
                xlabel={Number of refinements},
                ylabel={Throughput [DOF/s]},
                ymode=log,
                % ymin=0,
                % ymax =25,
                legend pos=north west]

            \newcommand{\addGMGplot}[1]{%
                \addplot+[thick] table[
                        col sep=comma,
                        x=refinements,
                        y expr=\thisrow{dofs}/\thisrow{gmg_time},
                        restrict expr to domain={\thisrow{p}}{#1:#1},
                        restrict expr to domain={\thisrow{smoothing}}{3:3},
                        restrict expr to domain={\thisrow{threshold}}{1:1},
                        restrict expr to domain={\thisrow{shyness}}{3:3},
                        unbounded coords=discard
                    ] {results/full_2D.csv}; \addlegendentry{$p=#1$}%
            }
            \addGMGplot{1}
            \addGMGplot{2}
            \addGMGplot{3}

            % Ensure `results/full_2D.csv` contains only rows with a numeric
            % `amg_time`. This calls an external script (requires -shell-escape).
            \immediate\write18{python3 scripts/filter_amg_time.py results/full_2D.csv results/tmp}
            \newcommand{\addAMGplot}[1]{%
                \addplot+[thick, mark=*, mark options={scale=0.8}, mark repeat=1] table[
                        col sep=comma,
                        x=refinements,
                        y expr=\thisrow{dofs}/\thisrow{amg_time},
                        restrict expr to domain={\thisrow{p}}{#1:#1},
                        restrict expr to domain={\thisrow{smoothing}}{3:3},
                        restrict expr to domain={\thisrow{threshold}}{1:1},
                        restrict expr to domain={\thisrow{shyness}}{3:3},
                        unbounded coords=discard
                    ] {results/tmp/full_2D.filtered.csv}; \addlegendentry{$p=#1$}% 
            }
            \addAMGplot{1}

        \end{axis}
    \end{scope}

    \begin{scope}[xshift=0.48\columnwidth]
        \begin{axis}[
                width=0.45\columnwidth,
                xlabel={Number of refinements},
                ylabel={Throughput [DOF/s]},
                ymode=log,
                legend pos=north west]
            \immediate\write18{python3 scripts/filter_amg_time.py results/full_3D.csv results/tmp}

            \newcommand{\addGMGplot}[1]{%
                \addplot+[thick] table[
                        col sep=comma,
                        x=refinements,
                        y expr= \thisrow{dofs}/\thisrow{gmg_time},
                        restrict expr to domain={\thisrow{p}}{#1:#1},
                        restrict expr to domain={\thisrow{smoothing}}{3:3},
                        restrict expr to domain={\thisrow{threshold}}{1:1},
                        restrict expr to domain={\thisrow{shyness}}{3:3},
                        unbounded coords=discard ] {results/full_3D.csv}; \addlegendentry{$p=#1$}%
            }

            \addGMGplot{1}
            \addGMGplot{2}
            \addGMGplot{3}

            % AMG plot for 3D
            \newcommand{\addAMGplotThree}[1]{%
                \addplot+[thick, mark=*, mark options={scale=0.8}, mark repeat=1] table[
                        col sep=comma,
                        x=refinements,
                        y expr=\thisrow{dofs}/\thisrow{amg_time},
                        restrict expr to domain={\thisrow{p}}{#1:#1},
                        restrict expr to domain={\thisrow{smoothing}}{3:3},
                        restrict expr to domain={\thisrow{threshold}}{1:1},
                        restrict expr to domain={\thisrow{shyness}}{3:3},
                        unbounded coords=discard
                    ] {results/tmp/full_3D.filtered.csv}; \addlegendentry{$p=#1$}%
            }
            \addAMGplotThree{1}

        \end{axis}
    \end{scope}
\end{tikzpicture}