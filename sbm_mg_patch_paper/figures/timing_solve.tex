\begin{tikzpicture}
    \begin{scope}
        \begin{axis}[
                width=0.45\columnwidth,
                xlabel={Number of refinements}, ylabel={Throughput [DOF/s]}, ymode=log, paperplot,
                % ymin=0,
                ymax =3e6 ]

            \newcommand{\addGMGplot}[1]{%
                \addplot+[thick] table[
                        col sep=comma,
                        x=refinements,
                        y expr=\thisrow{dofs}/\thisrow{gmg_time},
                        restrict expr to domain={\thisrow{p}}{#1:#1},
                        restrict expr to domain={\thisrow{smoothing}}{3:3},
                        restrict expr to domain={\thisrow{threshold}}{1:1},
                        restrict expr to domain={\thisrow{shyness}}{3:3},
                        unbounded coords=discard
                    ] {results/full_2D.csv}; \addlegendentry{$p=#1$}%
            }
            \addGMGplot{1}
            \addGMGplot{2}
            \addGMGplot{3}

            % AMG plots are dashed via the `dashed` plot option below.
            % Reset cycle index so AMG uses the same colors (but dashed)
            % as the GMG curves which were plotted above.
            \pgfplotsset{cycle list shift=-3}

            % Ensure `results/full_2D.csv` contains only rows with a numeric
            % `amg_time`. This calls an external script (requires -shell-escape).
            \immediate\write18{python3 scripts/filter_amg_time.py results/full_2D.csv }
            \newcommand{\addAMGplot}[1]{%
                \addplot+[thick, dashed, mark=*, mark options={scale=0.8}, mark repeat=1] table[
                        col sep=comma,
                        x=refinements,
                        y expr=\thisrow{dofs}/\thisrow{amg_time},
                        restrict expr to domain={\thisrow{p}}{#1:#1},
                        restrict expr to domain={\thisrow{smoothing}}{3:3},
                        restrict expr to domain={\thisrow{threshold}}{1:1},
                        restrict expr to domain={\thisrow{shyness}}{3:3},
                        unbounded coords=discard
                    ] {results/tmp/full_2D.filtered.csv};
            }
            \addAMGplot{1}

            % Restore cycle-list shift
            \pgfplotsset{cycle list shift=0}

        \end{axis}
    \end{scope}

    \begin{scope}[xshift=0.48\columnwidth]
        \begin{axis}[
                width=0.45\columnwidth,
                xlabel={Number of refinements},
                ymode=log,
                legend pos=south east,
                paperplot]
            \immediate\write18{python3 scripts/filter_amg_time.py results/full_3D.csv}

            \newcommand{\addGMGplot}[1]{%
                \addplot+[thick] table[
                        col sep=comma,
                        x=refinements,
                        y expr= \thisrow{dofs}/\thisrow{gmg_time},
                        restrict expr to domain={\thisrow{p}}{#1:#1},
                        restrict expr to domain={\thisrow{smoothing}}{3:3},
                        restrict expr to domain={\thisrow{threshold}}{1:1},
                        restrict expr to domain={\thisrow{shyness}}{3:3},
                        unbounded coords=discard ] {results/full_3D.csv}; \addlegendentry{$p=#1$}%
            }

            % Add GMG curves first (match 2D pattern)
            \addGMGplot{1}
            \addGMGplot{2}
            \addGMGplot{3}

            % AMG plots dashed but reuse GMG colors via cycle list shift
            \pgfplotsset{cycle list shift=-3}

            \newcommand{\addAMGplotThree}[1]{%
                \addplot+[thick, dashed, mark=*, mark options={scale=0.8}, mark repeat=1] table[
                        col sep=comma,
                        x=refinements,
                        y expr=\thisrow{dofs}/\thisrow{amg_time},
                        restrict expr to domain={\thisrow{p}}{#1:#1},
                        restrict expr to domain={\thisrow{smoothing}}{3:3},
                        restrict expr to domain={\thisrow{threshold}}{1:1},
                        restrict expr to domain={\thisrow{shyness}}{3:3},
                        unbounded coords=discard
                    ] {results/tmp/full_3D.filtered.csv};
            }
            \addAMGplotThree{1}

            % Restore cycle-list shift
            \pgfplotsset{cycle list shift=0}

        \end{axis}
    \end{scope}
\end{tikzpicture}